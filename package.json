{
  "name": "glp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Root manifest so Vercel installs the API's dependencies. The Vercel Function at api/ resolves modules from the project root, not from server/, so the server's runtime dependencies have to be declared here. Kept in step with server/package.json.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0"
  }
}
